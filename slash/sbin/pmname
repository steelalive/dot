pmname () 
{ 
    local apk_name name;
    for apk_name in "$@";
    do
        name="$(/system/sbin/aapt dump badging $1 | grep application-label:)";
        name="${name#*:}";
        name="$(echo "$name" | tr -d ' ' | tr -d "'")";
        echo $name;
    done
}
pmname "$@"
