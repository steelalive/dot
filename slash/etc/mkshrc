# Copyright (c) 2010
# vim: filetype=sh:
#	Thorsten Glaser <t.glaser@tarent.de>
# This file is provided under the same terms as mksh.
#-
# Minimal /system/etc/mkshrc for Android
# [[ -e /sbin/terminfo ]] && TERMINFO=/sbin/terminfo
[[ -e /etc/terminfo ]] && TERMINFO=/etc/terminfo
[[ -e /data/dot/slash/etc/terminfo ]] && TERMINFO=/data/dot/slash/etc/terminfo
: "${TERM:=xterm-256color}" "${HOME:=/data/dot/root}" "${MKSH:=/data/dot/slash/sbin/bash}" "${HOSTNAME:=POWER}"
: "${SHELL:=$MKSH}" "${USER:=$(getprop ro.product.device)}" "${TMP:=/data/local/tmp}" "${TMPDIR:=$TMP}"
PS1='$ '
unset LD_PRELOAD
if test -x /data/dot/slash/sbin/bash; then
	BASH=/data/dot/slash/sbin/bash
	SHELL="$BASH"
fi
PS1='\?'
export TERM HOME MKSH HOSTNAME SHELL USER PATH TERMINFO
bashrc="/storage/????-????/.bashrc"
if test -f "$bashrc"; then
	. "$bashrc" || . "/sdcard/.bashrc" || touch "$bashrc"
fi
rwroot() {
	[[ -e /external_sd ]] && mount /dev/block/mmcblk0p31 /system_root || mount /dev/block/mmcblk0p31 /
	mount -o remount,rw /
	mount -o remount,rw /system
	mount -o remount,rw /system_root
}
mountdot() {
	mountpoint /dot && return
	rwroot
	rm /dot 2>/dev/null
	mkdir -p /dot
	mount /storage/????-????/dot.img /dot || mount /external_sd/dot.img /dot
}
mountdot
########## _INIT_ ###########
. /data/dot/slash/etc/mk || . /dot/mk || . mk || . /sbin/mk || . /etc/mk || . "$(find / -name mk | head -n1)"
for p in ~/.bin /sbin /data/sbin /data/dot/slash/sbin /system/system/bin /system/system/xbin /system/bin /system/xbin; do
	[[ -d $p/. ]] || continue
	[[ :$PATH: == *:$p:* ]] || PATH=$p:$PATH
done

unset p
